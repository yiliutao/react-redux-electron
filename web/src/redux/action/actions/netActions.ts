import { ActStruct } from './actConfig';

interface CommonIdMap {
}
const ActionIds: CommonIdMap = {
};

interface CommonActMap {
}

const Actions: CommonActMap = {};

export {
    CommonIdMap,
    CommonActMap,
    ActionIds,
    Actions,
}